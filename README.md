php-wallet
==========

Web wallet server.

### Getting Started

```bash
curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/bin --filename=composer
cd src
composer update
cd ..s
docker-compose up
```